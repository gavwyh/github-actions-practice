name: on-push-workflow
on: push
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
        - name: Get code
          uses: actions/checkout@v3
        - name: Lint code
          run: npm run lint
    test:
        needs: lint
        runs-on: ubuntu-latest
        steps:
        - name: Get code
          uses: actions/checkout@v3
        - name: Test code
          run: npm run test
    deploy:
        needs: [lint, test]
        runs-on: ubuntu-latest
        steps:
        - name: Get code
          uses: actions/checkout@v3
        - name: Build code
          run: npm run build
        - name: Deploy code
          run: echo "deploying"
